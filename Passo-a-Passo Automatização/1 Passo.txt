Login Success
Login Fail

Exemplo:
cy.visit('http://localhost:3000/heroes')
cy.get(selectorList.loginButton).eq(0).click()
cy.get(selectorList.loginField).type('admin@test.com')


Seletores:

const selectorList = {
    loginButton:'.undefined',
    loginField:'[name="email"]',



- Fazer pagina separada para Users
"const userData" = {
    "userSuccess": {
      "username":"admin@test.com",
      "password":"test123"

- Criar pasta Pages para separar as classes

LoginPages
import LoginPage from '../pages/loginPage.js'
export default LoginPage

** return selector

Função com o click separado
saveForm() {









